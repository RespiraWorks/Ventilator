language: cpp
compiler: gcc

sudo: required

dist: bionic
addons:
  apt:
    packages:
      - qtbase5-dev
      - qtdeclarative5-dev
      - libqt5charts5-dev
#      - qt5-default
#      - qttools5-dev-tools
#      - qttools5-dev

before_script:
  - mkdir build
  - cd build

script:
  - qmake ..
  - make

